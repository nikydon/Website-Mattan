// Prisma schema — populated in Brick 3
// This file is intentionally minimal until the database brick.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
